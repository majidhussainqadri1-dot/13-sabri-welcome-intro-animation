name: File 13 One-Time Four-Plan Update

on:
  push:
    branches: [audit/file-13-source-review]

permissions:
  contents: write

jobs:
  apply:
    runs-on: windows-2025
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683
        with:
          fetch-depth: 0
      - name: Materialize verified overlay
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          from pathlib import Path
          import base64, hashlib, os, tarfile
          chunks = sorted(Path('.github').glob('file13-update.part*'))
          encoded = ''.join(path.read_text(encoding='utf-8') for path in chunks)
          payload = base64.b64decode(encoded)
          expected = '4065eda6830bbee3b27b5a48135aa1574eb0ef7db6e3a14969918b54a1b3c408'
          actual = hashlib.sha256(payload).hexdigest()
          if actual != expected:
              raise SystemExit(f'overlay checksum mismatch: {actual}')
          archive = Path(os.environ['RUNNER_TEMP']) / 'file13-overlay.tar.gz'
          archive.write_bytes(payload)
          with tarfile.open(archive, 'r:gz') as package:
              package.extractall('.')
          # Restore the source-ledger generator explicitly because the historical delete list
          # removes its obsolete predecessor on case-insensitive Windows filesystems.
          generator = Path('tools/generate-source-checksums.py')
          generator.write_text("""#!/usr/bin/env python3
from pathlib import Path
import hashlib
root=Path(__file__).resolve().parents[1]; source=root/'sabri-welcome-intro'; out=root/'SOURCE-CHECKSUMS-1.1.0.sha256'
rows=[]
for p in sorted(x for x in source.rglob('*') if x.is_file()):
    rows.append(f\"{hashlib.sha256(p.read_bytes()).hexdigest()}  sabri-welcome-intro/{p.relative_to(source).as_posix()}\")
out.write_text('\\n'.join(rows)+'\\n',encoding='utf-8')
print(f'wrote {len(rows)} source checksums')
""", encoding='utf-8')
          for raw in Path('.github/file13-delete-list.txt').read_text(encoding='utf-8').splitlines():
              if raw.strip() and raw.strip() != 'tools/generate-checksums.py':
                  Path(raw.strip()).unlink(missing_ok=True)
          Path('tools/generate-checksums.py').unlink(missing_ok=True)
          for path in chunks:
              path.unlink(missing_ok=True)
          Path('.github/file13-delete-list.txt').unlink(missing_ok=True)
          Path('.github/workflows/file13-bootstrap-update.yml').unlink(missing_ok=True)
          PY
          python tools/generate-pot.py
          python tools/generate-source-checksums.py
      - name: Commit final four-plan source
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "majidhussainqadri1-dot"
          git config user.email "majidhussainqadri1@gmail.com"
          git add -A
          git commit -m "feat(file-13): complete four-plan reviewed 1.1.0"
          git push origin HEAD:audit/file-13-source-review
