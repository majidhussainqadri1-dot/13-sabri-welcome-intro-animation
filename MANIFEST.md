# Release Manifest

- Module: File 13 — Welcome Intro Animation
- Runtime version: `1.1.0`
- Schema version: `1.1.0`
- Experience contract/version: `1.1.0`
- Canonical source folder: `sabri-welcome-intro/`
- Canonical ZIP folder: `sabri-welcome-intro-13/`
- Canonical archive: `release/13-sabri-welcome-intro-animation-1.1.0.zip`
- Main file: `sabri-welcome-intro.php`
- Database tables: none.
- External runtime services required by File 13 itself: none.
- Remote fonts/scripts/styles: none.
- Background jobs owned by File 13: none.
- Secrets owned by File 13: none.
- Destructive uninstall: disabled unless `SWI_PURGE_ON_UNINSTALL === true`.

## Data stores

File 13 uses bounded/minimized WordPress storage only:

- versioned configuration option;
- bounded configuration rollback snapshots option;
- bounded configuration audit option;
- optional bounded aggregate metrics option;
- short-lived configuration write lock option;
- account recurrence timestamp/config version/experience version metadata;
- account Never Show Again metadata;
- account File-13-only accessibility-profile metadata;
- guest/device session, recurrence timestamp, multi-tab claim, Never Show Again and accessibility-profile first-party keys.

It does not own a duplicate identity table, shell table, design-token table or service-worker registry.

## Future Welcome Experience Superset

The 1.1.0 package contains all 18 approved Future enhancements and their runtime/admin/privacy/health/rollback adapters. The exact executable list is exposed by `swi_get_welcome_intro_contract()` and checked by `tests/future-superset-regressions.php`.

## Deterministic evidence

- Exact-head build manifest: `MANIFEST-1.1.0.json` generated by `tools/build-release.py` during CI.
- Exact-head release digest: `release/SHA256SUMS` generated during the same build.
- Reproducibility check: `tests/reproducible-package.test.py`.
- Sequential review gate: `tests/eighty-round-review.py`.
- SBOM: `SBOM.spdx.json` (must describe current release metadata, not historical package evidence).

Historical `MANIFEST-1.0.0.json` / `CHECKSUMS-1.0.0.sha256` may remain only as historical evidence and must never be presented as the current 1.1.0 package checksum or exact-head proof.
